Install gitlab
#виділити ресурси (10000мб оперативи, 6 ядер проц )
#https://about.gitlab.com/install/#ubuntu

apt update
apt upgrade -y

apt-get install -y curl openssh-server ca-certificates tzdata perl
apt-get install -y postfix
#postfix =>internet site=>IP_VM(192.168.0.100)


# видаляємо апач і нджинкс (за необхідності)
# apt-get remove --purge apache2 apache2-data apache2-utils -y
# apt-get autoremove -y
# apt-get remove nginx -y
# apt-get autoremove -y

reboot
#перезагружаємо вм для встановлення оновлень

 curl https://packages.gitlab.com/install/repositories/gitlab/gitlab-ee/script.deb.sh | sudo bash
 EXTERNAL_URL="http://VM_IP(192.168.0.100)" apt install gitlab-ee

gitlab-ctl reconfigure

#/etc/gitlab/initial_root_password   пароль
#лог root
#Pv5SbnZjAdvHJdKT6zPzxsN1sLyHsr/1i6h4MNwzmew=

#перевірка статусу 
#sudo gitlab-ctl status
